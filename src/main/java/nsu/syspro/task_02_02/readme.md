`Здесь и далее cs == context switch`

                            Рассмотрим код потока А:

    (1): A_flag меняется только в коде потока А.

    (2): A_flag может быть опущен только перед инструкцией А1 или после А5. 
         т.к. после А5 по циклу возвращаемся в позицию "перед А1", 
         то будем считать, что A_flag может быть опущен только перед А1, 
         то есть в остальных местах A_flag поднят!
<br>

                            Рассмотрим код потока В:

    (3): В_flag меняется только в коде потока В.

    (4): В_flag может быть опущен только перед инструкцией В1 или после В3/В4/В7. 
         т.к. после В7 по циклу возвращаемся в позицию "перед В1", 
         то будем считать, что В_flag может быть опущен только в трёх ситуациях:
         (4.1) перед В1
         (4.2) после В3
         (4.3) после В4
         то есть в остальных местах В_flag поднят!
<br>

==========================================================================

                                        2.а
    (у): допустим, что мы впервые(!!!)(то есть ранее такой ситуации не случалось) 
         в обоих потоках зашли в critical_section, то есть последние команды, 
         что были исполнены, это А4 и В6.
<br>

                    Снова рассмотрим код потока В[выполняется (у)]:

    (6): чтобы зашли в В6, надо, чтобы не заходили(или вышли) в(из) цикл(а) В2.
         для этого надо чтобы А_flag был опущен, то есть[по правилу (2)] 
         поток А остановился на позиции "перед А1". заметим, что по правилу (4) 
         В_flag поднят. далее может быть две ситуации(после В2):

         (6.1): случился cs, то есть начал работать А. далее три пути:
                (6.1.1): исполнились А1, А2. зациклился на А2. то есть, пока 
                         поток В не выполнит В7[что противоречит (у)], поток А 
                         будет крутиться на месте.
                (6.1.2): исполнилось А1, случился cs, то есть начал работать В. 
                         тогда, пока В не начал или пока не закончил В6, надо 
                         чтобы произошёл cs, чтобы А "попытался" добраться до A4,
                         иначе будет нарушено (у). тогда А2 и попадаем в (6.1.1).
                (6.1.3): не успела исполниться А1, снова случился cs. 
                         по аналогии (6.1.2) попадаем в (6.1.2) или в (6.1.1).
                то есть данная ситуация (6.1) противоречит (у).
        (6.2): поток В начал исполнять В6. попадаем в аналогичную ситуацию (6.1).
        
        то есть (у) не выполняется, => невозможно(т.к. мы рассматривали ситуацию, 
        произошедшую впервые), чтобы оба потока исполняли свои critical_section.
<br>

==========================================================================

                                        2.b
    (д): допустим, что мы впервые(!!!)(то есть ранее такой ситуации не случалось)
         поймали deadlock, то есть А ждёт В, В ждёт А.

    (7): поток А может ждать В только если А ждёт, пока опустится В_flag. то есть
         поток А зациклился на А2. Заметим, что по правилу (2) А_flag поднят.
    (8): поток В может ждать А только если В ждёт, пока опустится А_flag. то есть
         поток В зациклился на:
         (8.1): В4. Заметим, что по правилу (4) В_flag опущен.
         (8.2): В2. Заметим, что по правилу (4) В_flag поднят.
    
    (7)+(8.1): поток А выйдет из цикла А2, т.к. В_flag опущен, => (д) ложно.
    (7)+(8.2): когда придёт исполнение потока В, он исполнит команду В3. далее
               два пути:
               (9.1): случился cs. получилась ситуация, аналоничная (7)+(8.1).
               (9.2): исполнится В4. попадаем в точности в ситуацию (7)+(8.1).
    
    то есть такой ситуации достичь невозможно(т.к. мы рассматривали ситуацию, 
    произошедшую впервые).
<br>
