(1) заметим, что в `z` записывает только 1 инструкция(`B4`). 
в `y` аналогично(`A3`). в `x` аналогично(`B2`, `C4`).

(2) заметим, что инструкции `C2`, `C3` и `C4` исполнятся только тогда, 
когда `у==2` на момент исполнения `C1`. 

(3) из (1)+(2) следует, что `a_x == 1` и `a_z == 1` на момент исполнения `А3`. 

(4) заметим, что для `a_x == 1` надо `А1` исполнилось позже `В1` и `В2`.

(5) заметим, что для `a_z == 1` надо `А2` исполнилось позже `В3` и `В4`.

(6) из (1)+(2)+(3)+(4)+(5) получаем, что получается не так много комбинаций(других нет):
1) `B1->B2->A1->B3->B4->A2->A3->C1->C2->C3->C4`
2) `B1->B2->B3->A1->B4->A2->A3->C1->C2->C3->C4`
3) `B1->B2->B3->B4->A1->A2->A3->C1->C2->C3->C4`


(6) каждая из которых в итоге выдаёт результат `{x=0; y=2; z=1}`.

**ЗАМЕЧАНИЕ: здесь и далее будем рассматривать ситуации, когда (-2), 
тобеж все оставшиеся комбинации эквивалентны тем, что начинаются с `C1->C2->...`_(для удобства я писать не буду)_, 
что эквивалентно тому, что в `х` записывает только 1 инструкция(`B2`)**

_ОБОЗНАЧЕНИЕ: запись `(-2)` означает, что условие `(2)` **не выполнено**_

(7) из (1)(и замечания выше!) следует, что на выходе программы `x == 1`, `z == 1`. 

(8) так, для результата `у` остаются 4 варианта(т.е. каждый из *[всех оставшихся после замечания]* вариантов эквивалентен одному из следующих):
1) (1)+(-4)+(-5), например: `A1->A2->B1->B2->B3->B4->A3` => `y==0`
2) (1)+(-4)+(5) ,  например: `A1->B1->B2->B3->B4->A2->A3` => `y==1`
3) (1)+(4)+(-5) , например: `B1->B2->A1->A2->B3->B4->A3` => `y==1`
4) (1)+(4)+(5)  , например: `B1->B2->A1->B3->B4->A2->A3` => `y==2`


ИТОГ: всего возможно 4 варианта:
1) `B1->B2->A1->B3->B4->A2->A3->C1->C2->C3->C4` => `{x=0; y=2; z=1}`
2) `C1->C2->A1->B1->B2->B3->B4->A2->A3` => `{x=1; y=0; z=1}`
3) `C1->C2->B1->B2->A1->A2->B3->B4->A3` => `{x=1; y=1; z=1}`
4) `C1->C2->B1->B2->A1->B3->B4->A2->A3` => `{x=1; y=2; z=1}`